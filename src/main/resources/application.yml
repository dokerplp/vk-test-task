spring:
  datasource:
    url: jdbc:postgresql://${HOST}:${BDPORT}/${NAME}
    username: ${USER}
    password: ${PASS}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true

app:
  pepper:
    ${PEPPER}